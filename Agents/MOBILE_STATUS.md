# MOBILE_STATUS.md - Отчет о состоянии iOS клиента

## Общий Статус
**Текущее состояние:** `ALPHA` (Есть архитектурный каркас, но не хватает критически важных функций для сдачи).

Проект написан на **Swift** с использованием **The Composable Architecture (TCA)** и **Tuist**, что является современным и надежным стеком. Архитектура модульная, код чистый.

Однако, **не реализованы** обязательные требования из задания (`subject.txt`), без которых проект **не будет принят**.

---

## Детальный Анализ (по пунктам Subject.txt)

### 1. Архитектура (✅ Отлично)
*   Используется **Tuist** для управления проектом (Targets).
*   **The Composable Architecture (TCA)** для управления состоянием.
*   Код разбит на модули (`AppFeature`, `AuthenticationFeature`, `SettingsFeature`, `MusicRoomAPI`).

### 2. Функциональность "Remote Control" (⚠️ Частично)
*   В `MusicRoomAPIClient.swift` видно, что клиент сделан как обертка над сетевыми запросами.
*   **Проблема:** URL бэкенда пока **захардкожен** (`http://localhost:8080/mock/events`), вместо того чтобы брать его из конфигурации.

### 3. Настраиваемый URL Бэкенда (❌ КРИТИЧНО)
*   **Требование:** "Back-end’s address must be configurable on the application".
*   **Реализация:** В коде `MusicRoomAPIClient.swift` стоит прямой TODO:
    ```swift
    // TODO: Retrieve configured backend URL from dependencies or configuration
    ```
    В `SettingsFeature` логика изменения URL, вероятно, планировалась, но API клиент с ней не связан. Это **блокирует сдачу**.

### 4. Аутентификация (⚠️ Неполная)
*   **Требование:** Login/Register (Email/Pass) + Social (Google/Facebook).
*   **Реализация:**
    *   ✅ Email/Password вход и регистрация есть в `AuthenticationFeature.swift`.
    *   ❌ **Social Login (Google/Facebook)** в коде `AuthenticationFeature` **не обнаружен**. Есть только поля `email` и `password`.

### 5. Логирование (❌ Отсутствует)
*   **Требование:** "Any action on the mobile application must generate logs on the back-end" (Platform, Device, Version).
*   **Реализация:** В `MusicRoomAPIClient.swift` нет логики отправки заголовков `X-Device-Model`, `X-App-Version` или вызовов эндпоинта логов.

### 6. Основные Сервисы (Голосование, Плейлисты)
*   В коде есть структуры для `Event`, `Playlist`, `Track`, но реализация полноценного взаимодействия (WebSocket, Delegation) требует более глубокого погружения. Судя по наличию `RealtimeMocks`, работа над этим велась, но статус интеграции под вопросом.

---

## Что нужно доделать (Рекомендации)

### 🔴 1. Настраиваемый URL (Приоритет: Блокирующий)
**Что сделать:**
*   В `SettingsFeature` (экран настроек при запуске) сохранить URL в `UserDefaults` / `Keychain`.
*   В `MusicRoomAPIClient` читать этот URL при каждом запросе.
*   **Почему:** Проверяющий не сможет подключить приложение к своему локальному серверу бе этого.

### 🔴 2. Социальная Авторизация (Приоритет: Высокий)
**Что сделать:**
*   Добавить кнопки "Войти через Google".
*   Реализовать вызов `ASWebAuthenticationSession` или SDK.
*   **Почему:** Обязательное требование Subject.txt.

### 🟠 3. Логирование действий
**Что сделать:**
*   Добавить Middleware или интерсептор в API Client, который добавляет заголовки устройства ко всем запросам.

### 🟠 4. Убрать Mock-данные
**Что сделать:**
*   Убедиться, что приложение работает с реальным бэкендом, а не с `MockDataFactory` в production-сборке.
