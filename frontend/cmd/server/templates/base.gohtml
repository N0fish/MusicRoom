{{ define "base" -}}
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>MusicRoom</title>

    <script src="https://unpkg.com/htmx.org@1.9.12"></script>
    <script src="https://cdn.tailwindcss.com?plugins=forms"></script>

    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: "var(--color-primary)",
              "primary-hover": "var(--color-primary-hover)",

              text: "var(--color-text)",
              "text-muted": "var(--color-text-muted)",

              background: "var(--color-background)",
              "card-bg": "var(--color-card-bg)",

              "input-bg": "var(--color-input-bg)",
              "input-border": "var(--color-input-border)",
              "input-focus": "var(--color-input-focus)",
              "input-ring": "var(--color-input-ring)",

              success: "var(--color-success)",
              error: "var(--color-error)",
            }
          }
        }
      }
    </script>

    <link href="/static/main.css" rel="stylesheet" />
  </head>

  <body class="bg-background min-h-screen text-text">
    <header class="sticky top-0 z-10 bg-card-bg/90 backdrop-blur border-b border-input-border">
      <div class="max-w-5xl mx-auto px-4">
        <div class="flex items-center justify-between h-16">
          <a class="text-2xl font-bold text-primary" href="/">MusicRoom</a>

          <nav id="nav-links" class="hidden md:flex gap-8 text-text-muted">
            <a class="{{if eq .Path "/" }}text-primary{{end}}" href="/">Home</a>
            <a id="nav-auth" class="{{if eq .Path "/auth" }}text-primary{{end}}" href="/auth">Auth</a>
            <a class="{{if eq .Path "/playlists" }}text-primary{{end}}" href="/playlists">Playlists</a>
            <a class="{{if eq .Path "/event" }}text-primary{{end}}" href="/event">Event</a>
            <a class="{{if eq .Path "/realtime" }}text-primary{{end}}" href="/realtime">Realtime</a>
            <a id="nav-me" class="{{if eq .Path "/me" }}text-primary{{end}}" href="/me" style="display: none;">Me</a>
            <a id="nav-logout" href="#" style="display: none;">Logout</a>
          </nav>

          <div class="text-xs text-text-muted">
            API: {{ .API }} &nbsp; WS: {{ .WS }}
          </div>
        </div>
      </div>
    </header>

    <script>
      const token = localStorage.getItem("token");
      const navAuth = document.getElementById("nav-auth");
      const navMe = document.getElementById("nav-me");
      const navLogout = document.getElementById("nav-logout");

      function logout() {
        localStorage.removeItem("token");
        window.location.href = "/auth";
      }

      if (token) {
        navAuth.style.display = "none";
        navMe.style.display = "block";
        navLogout.style.display = "block";
        navLogout.addEventListener("click", logout);
      } else {
        navAuth.style.display = "block";
        navMe.style.display = "none";
        navLogout.style.display = "none";
      }
    </script>

    <main class="max-w-5xl mx-auto px-4 py-6">
      {{ template "content" . }}
    </main>
  </body>
</html>
{{- end }}
